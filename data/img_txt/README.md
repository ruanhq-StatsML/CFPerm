# Drop the four CFPerm_Python feature files here:

- img_feats.npy
- txt_feats.npy
- labels.npy
- df_metadata.csv

Then run:

```bash
python3 scripts/run_img_txt_fsds.py
```

Or set `CFPERM_PYTHON_DIR=/path/to/CFPerm_Python` if the files live under that folder / `data/`.
