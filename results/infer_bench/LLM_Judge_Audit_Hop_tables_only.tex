% LLM judge / audit-logic hop. Requires booktabs.
% Numbers: HH-RLHF helpful-base OnlineRFPerm, gate γ=1.25, cut B4.

\begin{table}[ht]
\centering
\caption{Two layers for LLM audit. Smoothness is consecutive-window
changepoint; trustworthiness is degradation versus a frozen launch probe.
A slowly stricter policy can stay smooth; a overnight policy/judge swap is the hop.}
\label{tab:llm-audit-two-layers}
\small
\begin{tabular}{@{}p{3.4cm}p{4.2cm}p{5.2cm}@{}}
\toprule
Question & Statistic & Audit reading \\
\midrule
Is the audit chain still smooth? &
Last-two hop: \(e_{\mathrm{now}}/e_{\mathrm{prev}}\ge\gamma\) &
Same rubric, same judge: do adjacent batches still look like one map? \\
Is the auditor still trustworthy? &
RFPerm vs frozen launch probe &
Even a silky tightening can be caught before the old gold label is unusable. \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{RFPerm-as-Judge on Anthropic HH-RLHF helpful-base
(\(n=4000\), 50 batches \(\times\) 80, cut \(B_4\), \(\gamma=1.25\)).
Quiet \(t=2\) does not fire; the cut hop fires in the same window (delay 0).
Judge error \(0.147\to 0.539\) is the regime, not ``the judge suddenly became stupid.''}
\label{tab:llm-audit-hh}
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{@{}l c c c c@{}}
\toprule
Window & Fired & Ratio & Judge err / mix & Reading \\
\midrule
Quiet \(t=2\) & no & \(1.03\) & pos-rate \(0.48\) & audit map still smooth \\
Cut \(t=4\) & yes & \(1.34\) & pos-rate \(0.59\) & preference map hopped \\
Post-cut regime & --- & \(3.67\) & \(0.147\to 0.539\) & new audit law, then can be smooth again \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{What moved on the HH-RLHF cut. Style-domain AUC flags a \(P(X)\)
register shift; last-two fire + judge-err ratio flags \(P(Y\mid X)\).
Text-domain AUC near \(1/2\) says this was not a bag-of-words topic swap.}
\label{tab:llm-audit-axes}
\small
\begin{tabular}{@{}l c l@{}}
\toprule
Axis & Number & Action if this axis dominates \\
\midrule
Audit map \(P(Y\mid X)\) & hop ratio \(1.34\); err ratio \(3.67\) &
do not treat current judge as gold; Top-\(k\) \(\texttt{po\_risk0}\) re-review; block DPO merge \\
Register \(P(X)\) & style AUC \(0.999\) &
refresh the portrait / routing features, not necessarily the rubric \\
Lexical mix & text AUC \(0.494\) &
no action from bag-of-words domain shift \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{Serving gate. Fire opens a contrast window; it is not a
violation classifier. \(\texttt{po\_risk0}\) only ranks rows after a hop.}
\label{tab:llm-audit-gate}
\small
\begin{tabular}{@{}l p{5.4cm} p{5.4cm}@{}}
\toprule
State & Meaning & Action \\
\midrule
quiet & consecutive audit logic holds &
standard path; merge prefs under the old gate; \(w=1\) \\
fire & audit logic hopped &
current judge is not gold; human Top-\(k\); refuse or cap DPO merge;
optional \(\sqrt{\mathrm{PO}}\) on \(T=1\) only \\
\bottomrule
\end{tabular}
\end{table}
