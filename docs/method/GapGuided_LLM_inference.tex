\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage[T1]{fontenc}
\graphicspath{{../../results/gap_guided_inference/}}
\title{Two-sample reading budget: same expert and context packing}
\author{CFPerm prototype}
\date{}
\begin{document}
\maketitle
\noindent Not TMLE and not causal. $W$ is a batch tag; $\hat e\approx P(W{=}1\mid X)$.
$\pi$ is a shrinkage prior over which human block localizes the two-sample
difference. It is not a treatment propensity and is not used to target $Y$.

\section{Same expert versus packing}
A \emph{same expert} is $\hat m=\arg\max\pi$, then $f\in\mathcal{G}_{\hat m}$
(functions of $X_{B_{\hat m}}$ only) for every query. That template does not
depend on $x$. An instance gate $\hat m(x)=\arg\max s_m(x)$ is a different object.

Context packing is one reader on concatenated raw blocks
$E=E(\pi)$, not an opinion pool of $\hat e_m$. Disabled channels are omitted,
not emitted as empty headers. Abstain means the gap is not localized: do not
drop a block (pack all, or HITL). Forced top-1 on a diffuse shift discards signal.

\section{Specialist $\hat e_m$ (one block, all rows)}
\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{budgeted_auc.png}
\caption{Frozen specialist AUC. $\pi$ picks one $m$ for the whole test set.}
\end{figure}

\section{Packed reader (one RF on concat $E$)}
\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{packed_auc.png}
\caption{Column-concat of $E(\pi)$. Pack-all is the abstain-from-dropping action.}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{gt_hit_rate.png}
\caption{Localization hit. Same-expert hit is $1\{\hat m=m^\star\}$, not a per-row mix.}
\end{figure}

Inject valence with a wide text block: VIMP hires the wide specialist;
$\pi$ hires valence and the packed reader matches the GT block.
Diffuse equal shift: pack-all beats one specialist.
Shuffle valence: hit collapses.

\end{document}
